# Skill Library

当前 Windows 产品化版本默认不启用技能库执行能力。

保留这个文档的原因：
- 说明发布包中为什么看得到“技能库”占位入口
- 明确当前版本不会执行外部未知脚本
- 为后续可控扩展预留文档位置

当前策略：
- 默认不导入外部技能
- 默认不执行外部脚本
- 默认不读取用户敏感文件
- 默认不暴露 API Key

如果后续版本重新启用技能库，建议仅支持：
- 静态提示模板
- 可审计的只读资源
- 明确风险等级与人工确认
